name: CI

on:
  push:
    branches: [main, dev]
  pull_request:
    branches: [main]

jobs:
  test:
    runs-on: ${{ matrix.os }}
    strategy:
      fail-fast: false
      matrix:
        os: [ubuntu-latest, windows-latest]
        python-version: ["3.11", "3.12"]

    steps:
      - uses: actions/checkout@v4

      - name: Set up Python ${{ matrix.python-version }}
        uses: actions/setup-python@v5
        with:
          python-version: ${{ matrix.python-version }}

      - name: Install dependencies
        run: |
          pip install --upgrade pip
          pip install -r requirements-dev.txt
          pip install -e ".[dev]"

      - name: Check dependency sync
        run: python tools/check_dependency_sync.py

      - name: Format check (black)
        run: black --check src/ tests/

      - name: Import order check (isort)
        run: isort --check-only src/ tests/

      - name: Lint (flake8)
        run: flake8 src/ tests/

      - name: Type check (mypy)
        run: mypy src/floudsonnx

      - name: Type check (pyright)
        run: pyright src/floudsonnx

      - name: Unit tests
        run: pytest tests/unit/ -v --tb=short

  integration:
    runs-on: ubuntu-latest
    if: github.event_name == 'push' && github.ref == 'refs/heads/main'
    steps:
      - uses: actions/checkout@v4

      - name: Set up Python 3.12
        uses: actions/setup-python@v5
        with:
          python-version: "3.12"

      - name: Install with export extras
        run: |
          pip install --upgrade pip
          pip install -r requirements-dev.txt
          pip install -e ".[dev,export,seq2seq]"

      - name: Integration tests
        run: pytest tests/integration/ -v --tb=short -m integration
        env:
          HF_TOKEN: ${{ secrets.HF_TOKEN }}
          FLOUDSONNX_EXPORT_HF_TOKEN: ${{ secrets.HF_TOKEN }}
